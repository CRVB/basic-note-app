# Yazboz Note (macOS SwiftUI)

## Quick test flow

Build:

```bash
make build
```

Run app:

```bash
make run
```

During test:
- Open quick capture with `Cmd+Shift+K`
- Save quick note with `Enter`
- Close quick capture with `Esc`

Stop app:
- `Cmd+Q` from app window
- Or `Ctrl+C` in terminal
